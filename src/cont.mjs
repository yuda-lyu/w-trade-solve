/**
 * 共用常數
 *
 * dlmSeps為tid與sid內各段(mode與各key或各cond)之分隔字串
 * dlmPkgs為策略檔名內各段(tid、levelNumTrade、rEquivalentCumuProfitOrLossFinalNormYear)之分隔字串
 *
 * @name cont
 * @const
 * @type {Object}
 * @example
 *
 * console.log(cont)
 * // => { dlmSeps: ' ║ ', dlmPkgs: ' ⊙ ' }
 *
 */
let r = {
    dlmSeps: ' ║ ',
    dlmPkgs: ' ⊙ ',
}


export default r
